#include<iostream>
#include<vector>
#define SIZE 1000
using namespace std;

int main(){
    int A, B;
    vector<int> a(A), b(B);
    vector<bool> a_is_empty(SIZE, false);
    cin >> a >> b;
    for(int i=0; i<a; i++) cin >> a.at(i);
    for(int i=0; i<b; i++) cin >> b.at(i);


}